PKG_CPPFLAGS = -w -Wno-deprecated -DNDEBUG -DUNORDERED -I. -I../inst/include/LASlib -I../inst/include/LASzip

SOURCES = $(wildcard ./*.cpp LASlib/*.cpp LASzip/*.cpp)

OBJECTS =  $(SOURCES:.cpp=.o)

all: $(SHLIB)

clean:
	@rm -f $(OBJECTS)